SET_RESOURCE_URI_FROM_JSON(
    SANDBOX_RESOURCE_URI
    resources.json
)

IF (SANDBOX_RESOURCE_URI)
    ENABLE(PREBUILT)
    PREBUILT_PROGRAM(install_node_modules)
    DECLARE_EXTERNAL_RESOURCE(NOTS_INSTALL_NODE_MODULES ${SANDBOX_RESOURCE_URI})
    PRIMARY_OUTPUT(${NOTS_INSTALL_NODE_MODULES_RESOURCE_GLOBAL}/install_node_modules)
    END()

ENDIF()
